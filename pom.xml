<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example</groupId>
  <artifactId>xml-reader</artifactId>
  <version>0.1.0</version>
  <name>xml-reader</name>
  <description>Exemplo Maven Java 21: ler XML, Uber-JAR, GraalVM Native Image e jpackage</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>
    <b2acsl.mock>false</b2acsl.mock>

    <jackson.version>2.17.2</jackson.version>
    <graalvm.native.plugin.version>0.10.3</graalvm.native.plugin.version>
    <maven.shade.plugin.version>3.6.0</maven.shade.plugin.version>
    <maven.compiler.plugin.version>3.13.0</maven.compiler.plugin.version>
    <maven.antrun.plugin.version>3.1.0</maven.antrun.plugin.version>
    <graalvm.home>${env.GRAALVM_HOME}</graalvm.home>

    <!-- SonarQube: análise local via "mvn sonar:sonar" contra um servidor em
         sonar.host.url (default: instância local na porta padrão do SonarQube).
         Token NÃO fica aqui (segredo) — passar via -Dsonar.token=... na linha de
         comando ou variável de ambiente SONAR_TOKEN. -->
    <sonar.maven.plugin.version>3.11.0.3922</sonar.maven.plugin.version>
    <sonar.projectKey>b2acsl-plugin</sonar.projectKey>
    <sonar.projectName>B2ACSL Plugin</sonar.projectName>
    <sonar.host.url>http://localhost:9000</sonar.host.url>
    <sonar.sources>src/main/java</sonar.sources>
    <sonar.java.source>${maven.compiler.release}</sonar.java.source>
  </properties>

  <dependencies>
    <!-- Jackson XML -->
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-xml</artifactId>
      <version>${jackson.version}</version>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
        <includes>
          <include>META-INF/b2acsl.properties</include>
        </includes>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>false</filtering>
        <excludes>
          <exclude>META-INF/b2acsl.properties</exclude>
        </excludes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>

      <!-- SonarQube: opt-in, só roda com "mvn sonar:sonar" (sem <executions>, não entra
           no build normal). Precisa de um servidor SonarQube acessível em sonar.host.url
           e um token (-Dsonar.token=... ou variável SONAR_TOKEN). -->
      <plugin>
        <groupId>org.sonarsource.scanner.maven</groupId>
        <artifactId>sonar-maven-plugin</artifactId>
        <version>${sonar.maven.plugin.version}</version>
      </plugin>

      <!-- Uber-JAR (necessário para jpackage) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven.shade.plugin.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <shadedArtifactAttached>true</shadedArtifactAttached>
              <shadedClassifierName>all</shadedClassifierName>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>com.example.Main</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- GraalVM Native Image -->
      <plugin>
        <groupId>org.graalvm.buildtools</groupId>
        <artifactId>native-maven-plugin</artifactId>
        <version>${graalvm.native.plugin.version}</version>
        <extensions>true</extensions>
        <configuration>
          <imageName>${project.artifactId}</imageName>
          <mainClass>com.example.Main</mainClass>
        </configuration>
      </plugin>

    </plugins>
  </build>

  <profiles>
    <profile>
      <id>native</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.graalvm.buildtools</groupId>
            <artifactId>native-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>build-native</id>
                <phase>package</phase>
                <goals>
                  <goal>compile-no-fork</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>native-gui</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.graalvm.buildtools</groupId>
            <artifactId>native-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>build-native-gui</id>
                <phase>package</phase>
                <goals>
                  <goal>compile-no-fork</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <imageName>${project.artifactId}</imageName>
              <mainClass>com.example.Main</mainClass>
              <buildArgs>
                <buildArg>-Djava.awt.headless=false</buildArg>
                <buildArg>-H:+ReportExceptionStackTraces</buildArg>
              </buildArgs>
            </configuration>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-antrun-plugin</artifactId>
            <version>${maven.antrun.plugin.version}</version>
            <executions>
              <execution>
                <id>package-native-gui-dist</id>
                <phase>package</phase>
                <goals>
                  <goal>run</goal>
                </goals>
                <configuration>
                  <target>
                    <condition property="resolved.graalvm.home" value="${graalvm.home}" else="${java.home}">
                      <and>
                        <isset property="graalvm.home"/>
                        <not>
                          <equals arg1="${graalvm.home}" arg2=""/>
                        </not>
                      </and>
                    </condition>

                    <fail message="Could not resolve GraalVM home. Use -Dgraalvm.home=/path/to/graalvm or set GRAALVM_HOME.">
                      <condition>
                        <equals arg1="${resolved.graalvm.home}" arg2=""/>
                      </condition>
                    </fail>

                    <mkdir dir="${project.build.directory}/dist"/>
                    <mkdir dir="${project.build.directory}/dist/runtime/lib"/>
                    <mkdir dir="${project.build.directory}/dist/runtime/lib/server"/>

                    <copy file="${project.build.directory}/${project.artifactId}"
                          tofile="${project.build.directory}/dist/${project.artifactId}"/>

                    <copy file="${resolved.graalvm.home}/lib/libawt.so"
                          tofile="${project.build.directory}/dist/runtime/lib/libawt.so"/>
                    <copy file="${resolved.graalvm.home}/lib/libawt_xawt.so"
                          tofile="${project.build.directory}/dist/runtime/lib/libawt_xawt.so"/>
                    <copy file="${resolved.graalvm.home}/lib/libawt_headless.so"
                          tofile="${project.build.directory}/dist/runtime/lib/libawt_headless.so"/>
                    <copy file="${resolved.graalvm.home}/lib/libjawt.so"
                          tofile="${project.build.directory}/dist/runtime/lib/libjawt.so"/>
                    <copy file="${resolved.graalvm.home}/lib/server/libjvm.so"
                          tofile="${project.build.directory}/dist/runtime/lib/server/libjvm.so"/>

                    <echo file="${project.build.directory}/dist/run-gui.sh"><![CDATA[#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "$0")" && pwd)"

export LD_LIBRARY_PATH="$DIR/runtime/lib/server:$DIR/runtime/lib:${LD_LIBRARY_PATH:-}"
export JAVA_AWT_HEADLESS=false

if [ -n "${DISPLAY:-}" ]; then
  exec "$DIR/${project.artifactId}" "$@"
fi

if command -v xvfb-run >/dev/null 2>&1; then
  exec xvfb-run "$DIR/${project.artifactId}" "$@"
fi

echo "No graphical DISPLAY and xvfb-run not found."
echo "Install xvfb or run in a graphical session."
exit 1
]]></echo>
                    <chmod file="${project.build.directory}/dist/run-gui.sh" perm="755"/>
                    <chmod file="${project.build.directory}/dist/${project.artifactId}" perm="755"/>
                  </target>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
